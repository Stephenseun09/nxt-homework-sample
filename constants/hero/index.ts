const heroHeading = "The Trail-blazers in Virtual Healthcare."; // hero heading
const heroDescription =
  "We are the premier virtual hospital that provides very affordable and optimal healthcare for everyone for less than a dollar!"; // hero description

const heroImg = "/images/heroImg.png"; // relative path to image

const heroData = { heroHeading, heroDescription, heroImg };

export default heroData;
